Определяем, что объект - это ЛОД(14).
Если истина, то текстуры, prop/prop_fake_kollision.dds := trees/trees_fake.dds

В конце, генерируем файл, где указано, сколько и какие текстуры не были настроены(пути)(txt)